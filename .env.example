# Optional: any OpenAI-compatible chat-completions endpoint.
# Without these values, the app uses its deterministic offline teaching response.
LLM_API_KEY=
LLM_MODEL=
LLM_BASE_URL=https://api.openai.com/v1
